(function ($, Drupal) {
  Drupal.behaviors.eventsChallengeBehavior = {
    attach: function (context, settings) {
      console.log('Events Challenge theme JavaScript works!');
    }
  };
})(jQuery, Drupal);
